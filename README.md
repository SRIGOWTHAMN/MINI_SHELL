# MINI_SHELL
Developed a Linux Mini Shell in C that executes user commands, supports built-in commands, process creation, and signal handling using Linux system calls; gained hands-on experience with fork(), exec(), wait(), command parsing, and process management.
